// Retired top-level page — folded into the Settings list per the redesign.
#![allow(dead_code)]

use eframe::egui::Ui;

pub fn show(ui: &mut Ui) {
    crate::placeholder::show(ui, "Integrations");
}
